type: :app # no need to change this for .pkg, .dmg and executables
os: :osx
name: CodeWhispererInputMethod.app
outputs:
  - label: macos
    path: CodeWhispererInputMethod.app
app:
  :identifier: com.amazon.inputmethod.codewhisperer # this has to be same as Bundle identifier value in info.plist
  signing_requirements:
    certificate_type: :developerIDAppDistribution
    app_id_prefix: 94KV3E626L # AMZN Mobile LLC
