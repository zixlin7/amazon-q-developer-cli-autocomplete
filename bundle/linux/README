To install the Amazon Q CLI run the install.sh script

./install.sh

To enable support for SSH if this is being installed on a remote machine,
run the following lines then restart sshd and reconnect to the machine.

AcceptEnv Q_SET_PARENT
AllowStreamLocalForwarding yes
