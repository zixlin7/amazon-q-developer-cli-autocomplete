version: v1
deps: []
build:
  excludes:
    - node_modules
breaking:
  use:
    - WIRE_JSON
lint:
  use:
    - STANDARD
  except:
    - ENUM_ZERO_VALUE_SUFFIX
    - PACKAGE_VERSION_SUFFIX
    - DIRECTORY_SAME_PACKAGE
    - PACKAGE_DIRECTORY_MATCH
